<header>
  <nav class="navbar navbar-expand-lg">
    <div class="container">
      <div class="collapse navbar-collapse mx-ou d-flex">
        <a class="navbar-brand img_wrap" href="/"><%= image_tag asset_path('logo1.png'), :size => '200x50' %></a>
        <div class="form-inline flex-grow-1">
          <input id='search-btn' type='checkbox'/>
          <label for='search-btn'></label>
          <form action="/search" method="get">
            <input id='search-bar' type='txet' name='search' placeholder='Search...'/>
          </form>
        </div>
        <div class="navbar-nav d-flex">
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to '投稿一覧', post_images_path, class:"fas fa-comments fa-lg edit-link outline btn" %>
            </li>
            <li class="nav-item">
              <%= link_to "Tweet", new_post_image_path, class:"fas fa-comment-medical fa-lg edit-link outline btn" %>
            </li>
            <li class="nav-item">
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class:"fas fa-sign-out-alt fa-lg edit-link outline btn" %>
            </li>
            <li class="nav-item">
            <%= link_to 'マイページ', user_path(current_user.id), class:"fas fa-user-circle fa-lg edit-link outline btn" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "ログイン", new_user_session_path, class:"fas fa-sign-in-alt fa-lg edit-link outline btn" %>
            </li>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
</header>

